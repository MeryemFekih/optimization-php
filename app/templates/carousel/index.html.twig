{% extends 'base.html.twig' %}

{% block title %}Hello CarouselController!{% endblock %}

{% block body %}

<div class="flex flex-col items-center space-y-12 ">
	<section class="flex flex-col items-center justify-center space-y-6">
		<h1 class="text-5xl">Bienvenue dans la Galaxie Boissières</h1>
		<h2 class="text-xl ">Ici gravitent les guitares Boissières qui ont changé d'orbite</h2>
	</section>
	{% for item in carousel %}
	<section class="flex justify-between ">
		<div class="flex flex-col  justify-center space-y-6 w-5/12">
			<h2 class="text-4xl uppercase">{{item.title | raw}}</h2>
			{{item.description | raw}}
		</div>
		<div class="flex flex-col items-center justify-center w-5/12 space-y-6">
			
			<img src="{{asset('img/' ~ item.files[0].filename_disk)}}" class="max-w-14 h-[640px] object-cover">

			<div class="flex space-x-6">
				{% for file in item.files %}
					{% if loop.index0 != 0 %}
						<img src="{{asset('img/' ~ file.filename_disk)}}" class="max-w-14 max-h-14 object-cover">
					{% endif%}
				{% endfor%}
			</div>
		</div>
	</section>
	{% endfor %}
</div>

{% endblock %}